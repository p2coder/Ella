[project]
name = "ella-runtime-mvp"
version = "0.1.0"
description = "Minimal root skeleton for the Ella Runtime MVP."
requires-python = ">=3.11"
dependencies = [
    "openai>=1.0,<2",
    "opencv-python>=4.12,<5",
    "sounddevice>=0.5,<1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
