# 历史架构图

本目录中的 `ella-architecture.*` 是已废弃（superseded）的历史生成产物，仅用于追溯旧设计及其视觉检查记录。图中出现的 DAG、route、presence、handoff、PlanStore 与 `plan_written` 均不是现役 Runtime 契约。

当前实现与设计以 [`docs/runtime_tools_workflow_prd.md`](../runtime_tools_workflow_prd.md) 为准。
